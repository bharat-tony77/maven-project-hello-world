pipeline {
    agent any
    stages {
        stage("pre_build") {
            steps{
                echo 'git repo cloned'
            }
        }
        stage("build") {
            steps{
                echo 'building maven packages'
            }
        }
        stage("post_build") {
            steps{
                echo 'running terraform commands'
            }
        }
    }
}


 